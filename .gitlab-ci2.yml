include:
  - project: 'cicd-template/build'
    ref: main
    file:
      - 'build_harbor.yaml'

stages:
  - build

build:
  extends: .build
  variables:
    DOCKER_FILE_PATH: .infra/docker/Dockerfile2
  only:
    - develop